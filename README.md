# BCD-to-7-segment-decoder
BCD to 7-Segment Decoder  A digital logic hardware project that implements a BCD (Binary Coded Decimal) to 7-Segment Decoder using combinational circuits and physical components.  This project demonstrates how binary inputs (0000–1001) can be converted into decimal digits (0–9) and displayed on a 7-segment LED display
